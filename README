Program to solve sudoku puzzle. Input to the program is a .csv file. Limited error checking is done to make sure 9x9 input is provided. 
If an ascii char is part of the board then it treated as 0. The program uses backtracking to find a solution. 
For each emppty value, check a number between 1..9 and see if it does not appear in the row, col or 3x3 base cell.
